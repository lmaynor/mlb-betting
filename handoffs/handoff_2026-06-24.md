# Handoff -- 2026-06-24 (parallel sessions)

Three sessions ran in parallel on 2026-06-24. Each owns one section below.
Fill your section; leave the others untouched to avoid merge collisions.

---

## Section A -- Model-health: calibration coverage + no-edge retirement

Goal: investigate why some systems consistently lose. Pulled ops data via
`/model-health`, `/edge-analysis`, `Gates/model_gates.json`, Cloud Run logs, and
direct `bets`-table queries (Cloud SQL proxy from Cloud Shell).

**Root cause.** Losses dominated by model overconfidence on large "edges"
(the documented adverse-selection curve). Acute fixable part: two live systems --
**PITCHER_ER** and **F1H** -- bypassed the prediction-calibration + `EDGE_CAP`
layer entirely. Post-06-11 they leaked the toxic big-edge bets every *wired*
system suppressed (PITCHER_ER placed 55/65 `>0.20`-edge bets, overconf +0.129 on
n=190; F1H 3/3, +0.113 on n=95). Calibration confirmed working where wired
(HR +0.034, OUTS +0.015 overconf). Also found: all prediction calibrators were
13 days stale (a retrain does NOT refit them), and HR never logged `book`.

**Weekly-trend reclassification (key nuance).** The rolling-30 gate can't tell a
consistent loser from one bad week. The 05-05..06-22 trend showed:
- True no-edge losers (AUC ~0.50, negative most/every week): **1IOU, F5, F1H**.
- Volatile / one-bad-week (do NOT retrain reactively): **OUTS** (positive 5 of 7
  weeks; -46% rolling is the single 06-22 week, n=10), **HR** (longshot variance,
  AUC 0.627), **K** and **BATTER_HITS** (healthy, one off week).

**Shipped + deployed.**
- PR #13 (merged) -- PITCHER_ER + F1H wired into pre-edge calibration + `EDGE_CAP`;
  HR `book` propagation; `docs/solutions/conventions/retrain-calibrate-sequence.md`
  documents the second (prediction) calibrator layer.
- PR #16 (merged) -- retire the no-edge set: `1IOU` + `F5` via registry
  `force_gate="on"`; `F1H` via `run_f5` `LOG_ONLY_SYSTEMS`. All still log
  predictions (kelly_triggered=False); reversible after retrain.
- Cloud Shell ops: re-fit all 10 prediction calibrators (`mlb-fit-calibrators`);
  created `mlb-fit-calibrators-weekly` scheduler (Mon 10:00 UTC).
- **Live on revision `mlb-betting-00235-gl4`** (deployed 2026-06-24 ~15:20 CST).
  `force_gate` is baked into the image -> suppression active on the next `/run`
  (22:00 UTC); betting effect only manifests then.

**Open for next session.**
1. Validate: `/edge-analysis` -- PITCHER_ER + F1H `>=20%` buckets should stop placing.
2. Retrain NRFI(1IOU) + F5, then clear the pauses (registry `force_gate=None`;
   remove `"F1H"` from `LOG_ONLY_SYSTEMS`). NRFI is live-inverted DESPITE the 06-22
   retrain (OOS AUC 0.589 vs live 0.498) => feature drift, not undertraining; only
   the `lineup` sub-model has signal (pitcher 0.526 / context 0.504 are dead).
   Investigate the Monday PSI drift monitor first; consider a lineup-only model.
3. OUTS: watch ~1 more week (do not retrain on n=10); gate is protecting it.
4. HR: variance/longshot problem, well-calibrated now; consider small-edge-only
   (the <5% edge bucket was +6.4% ROI).

Detailed write-up + reusable Cloud SQL/diagnostic snippets:
`handoffs/handoff_2026-06-24_calibration_coverage.md`. PRs: #13, #16.

---

## Section B -- Edge cockpit viz + enrichment + ParlayAPI markets + cost-opt

Goal: clear the pending items from `handoff_2026-06-16_beezy_platform.md` (Edge
enrichment deploy/validate, ParlayAPI cadence+scheduling) plus a cost-opt cleanup.

**Shipped + merged.** PRs #11, #12, #17, #18 (all merged to main); PR #1 closed (superseded).
- **#11 Edge cockpit viz** -- redesigned every `/edge` chart for a CASUAL viewer
  (`beezy-vip/app/edge/edge-client.tsx`): each leads with a plain-language takeaway,
  plus labeled axes / legends / summary-stat chips (EV-LA, spray, velo, release, zone,
  recent-form, probability track). Validated in a static SVG harness (the Next app
  can't boot locally -- Clerk needs keys); real render still wants a Vercel preview.
- **#11 OUTS sparkline fix** -- `build_edge_enrichment.py` was counting every PA-ending
  event as an out; now sums out-producing events w/ DP/TP multipliers.
- **#11 `deploy/setup_edge_enrichment.sh`** (new) -- enrichment Job + 2 schedulers at
  **16:20/22:20 UTC** (NOT after refresh-data -- enrichment needs scored picks, which
  exist only after the 16:00/22:00 betting runs).
- **#12 ParlayAPI MLB market keys** -- verified against a live payload: `player_outs`
  is NOT real (billed for 0 data); real outs keys are `player_pitcher_outs` +
  `player_pitching_outs`, both collapsed to short name `outs` via `_MARKET_ALIASES`
  in `parlay_extract`. Final MLB set = hits/total_bases/home_runs/strikeouts/earned_runs
  + the two outs keys (7 keys -> 6 markets).
- **#17 + #18 cost-opt** -- `build_batter_hits_features.py` now loads only ~18 statcast
  cols via `usecols` (the OOM fix). Dockerfile: uninstall nccl in the requirements RUN
  **and** `pip install -e . --no-deps` (#18 fixed #17's regression where `-e .` re-pulled
  the 300MB nccl back into the image).

**Deployed + verified (Cloud Shell).**
- Service rev `mlb-betting-00236-qjv`. (NOTE: built before #18 merged -> still carries the
  ~300MB nccl bloat; the **next** `deploy_service.sh` slims it -- confirm Step 20 no longer
  downloads nccl.)
- **batter_hits job lowered 4Gi -> 2Gi and ran clean** (exec kk8hp, 1/1) -> `usecols` works.
- **Edge enrichment VALIDATED** (exec brxsz): 18 players enriched -- batters got
  `ev_la/recent_form/spray`, pitchers got `velo/release/zone` (+`recent_form` for K/OUTS;
  ER skips it by design). Statcast identity matching confirmed for both paths.
- **ParlayAPI scheduled**: `parlay-accum-mlb-props` + `parlay-accum-mlb-game-lines` every
  4h (`0 */4 * * *`); `parlay-accum-nba-props` staged (no scheduler). All 6 markets.

**Open / watch for next session.**
1. **scheduler PERMISSION_DENIED (status code 7)** on the last cron auto-attempts of the
   edge + parlay jobs. Manual `gcloud run jobs execute` works (user creds); the SCHEDULED
   (OAuth) invocations may be failing. Verify after the next fire; if failing, grant the
   invoker SA run.invoker per job:
   `gcloud run jobs add-iam-policy-binding <job> --member=serviceAccount:scheduler-invoker@concrete-crow-445205-m4.iam.gserviceaccount.com --role=roles/run.invoker --region=us-central1`
2. **ParlayAPI tier**: user is upgrading to **$5 / 20k cr tomorrow (06-25)**. The every-4h
   jobs were scheduled tonight on the FREE tier (~857 cr) -> they will pause at the cap until
   the upgrade. Budget at 6 markets, full slate ~91 cr/refresh; ~16-20k/mo at every-4h.
   Watch `credits_remaining` in `OddsAccum/{sport}/latest.json`. NBA scheduler: wire in October.
3. **Leaner image**: next `deploy_service.sh` picks up #18 (~300MB smaller).
4. **ROTATE the GitHub PAT** used this session (appeared in the transcript).

Detailed runbook (Cloud Shell blocks, ParlayAPI verify-first flow):
`handoffs/handoff_2026-06-16_edge_cockpit_viz.md`. PRs: #11, #12, #17, #18.

---

## Section C -- (parallel session 3)

_TODO: parallel session to fill._
